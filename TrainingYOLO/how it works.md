1) Feed  the model with large data set of label images
2) then the model makes predictions on what is in the images
3) These predictions are then compared to the actual labels or contents to calculate errors
4) The calculated errors go through back-propagation, when the algorithm takes the error computed at the output and propagates it backwards through the network layers. This process also computes the gradient for every weight.![[Pasted image 20251230111935.png]]
